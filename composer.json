{
    "name": "phphd/exceptional-validation",
    "description": "Capture domain exceptions and map them to the corresponding properties that caused them",
    "type": "symfony-bundle",
    "license": "MIT",
    "authors": [
        {
            "name": "Yevhen Sidelnyk",
            "email": "zsidelnik@gmail.com"
        }
    ],
    "minimum-stability": "stable",
    "require": {
        "php": ">=8.1",
        "webmozart/assert": "^1.12.1",
        "phphd/exception-toolkit": "^1.2",
        "composer-runtime-api": "^2.0"
    },
    "conflict": {
        "symfony/validator": "<6.0 || >8.4",
        "symfony/http-kernel": "<6.0 || >8.4",
        "symfony/dependency-injection": "<6.2 || >8.4",
        "symfony/messenger": "<6.4 || >8.4"
    },
    "require-dev": {
        "symfony/validator": ">=6.0",
        "symfony/http-kernel": ">=6.0",
        "symfony/dependency-injection": ">=6.2",
        "symfony/messenger": ">=6.4",
        "symfony/uid": ">=6.0@dev",
        "symfony/var-dumper": ">=6.0",
        "phphd/coding-standard": "~0.7.0",
        "tomasvotruba/type-coverage": "^2.1.0",
        "phpstan/phpstan-phpunit": "^2.0.16",
        "nyholm/symfony-bundle-test": "^3.1",
        "phpstan/phpstan": "^2.1.40",
        "phpstan/phpstan-strict-rules": "^2.0.10",
        "phpat/phpat": "^0.11.10",
        "phpunit/phpunit": "^10.5.46",
        "phpbench/phpbench": "^1.2.6",
        "bamarni/composer-bin-plugin": "^1.8"
    },
    "suggest": {
        "symfony/validator": "Install Validator component to use constraint violations mapper",
        "symfony/messenger": "Install Messenger component to use exceptional validation middleware",
        "amphp/amp": "Install AMP package to capture multiple exceptions at once"
    },
    "replace": {
        "phphd/exceptional-validation-bundle": "*"
    },
    "repositories": [
        {
            "type": "vcs",
            "url": "https://github.com/phphd/coding-standard"
        }
    ],
    "autoload": {
        "psr-4": {
            "PhPhD\\": "src",
            "PhPhD\\ExceptionalValidation\\Upgrade\\": "upgrade"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "PhPhD\\ExceptionalValidation\\Tests\\": "tests"
        }
    },
    "scripts": {
        "post-update-cmd": [
            "composer bin psalm update --ansi"
        ],
        "ci:pack": [
            "@ci:ecs",
            "@ci:rector",
            "@ci:phpstan",
            "@ci:psalm",
            "@ci:unit-test",
            "@ci:integration-test"
        ],
        "ci:ecs": "vendor/bin/ecs check",
        "ci:ecs-fix": "vendor/bin/ecs check --fix",
        "ci:rector": "vendor/bin/rector process --dry-run",
        "ci:rector-fix": "vendor/bin/rector process",
        "ci:phpstan": "vendor/bin/phpstan analyze",
        "ci:psalm": "vendor-bin/psalm/psalm",
        "ci:test": "vendor/bin/phpunit --testdox --colors=always",
        "ci:unit-test": "vendor/bin/phpunit --testdox --colors=always --testsuite=Unit",
        "ci:integration-test": "vendor/bin/phpunit --testdox --colors=always --testsuite=Integration"
    },
    "config": {
        "allow-plugins": {
            "bamarni/composer-bin-plugin": true
        }
    },
    "extra": {
        "bamarni-bin": {
            "bin-links": false,
            "forward-command": true
        }
    }
}
