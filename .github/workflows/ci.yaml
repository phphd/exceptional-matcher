name: 'Continuous Integration'

on:
    pull_request: ~
    workflow_dispatch: ~
    push:
        branches: [ main ]

jobs:
    composer-validate:
        name: Composer Validate
        runs-on: ubuntu-latest
        steps:
            -   name: 'Checkout Code'
                uses: actions/checkout@v4

            -   name: 'Validate composer.json'
                run: composer validate --strict --ansi

    code-style:
        name: ${{ matrix.actions.name }}
        needs: composer-validate
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                actions:
                    -   name: 'Coding Standard'
                        run: vendor/bin/ecs check --no-progress-bar --ansi
                    -   name: 'Rector'
                        run: vendor/bin/rector process --dry-run --no-progress-bar --ansi
        steps:
            -   name: 'Checkout Code'
                uses: actions/checkout@v4

            -   name: 'Setup PHP'
                uses: shivammathur/setup-php@v2
                with:
                    php-version: 8.5
                    coverage: none
                    tools: composer:v2

            -   name: 'Install Dependencies'
                uses: "ramsey/composer-install@v2"
                with:
                    composer-options: "--optimize-autoloader"

            -   run: ${{ matrix.actions.run }}
    static-analysis:
        name: ${{ matrix.actions.name }} (${{ matrix.php }}, ${{ matrix.dependencies }})
        needs: composer-validate
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                php: [ '8.1', '8.5' ]
                dependencies: [ highest, lowest ]
                actions:
                    -   name: 'Psalm'
                        run: |
                            SHEPHERD_FLAG="--shepherd"
                            # On PHP 8.5 it causes deprecation as yet
                            if [ "$PHP_VERSION" = "8.5" ]; then
                                SHEPHERD_FLAG=""
                            fi
                            vendor-bin/psalm/psalm --stats --no-progress --output-format=github $SHEPHERD_FLAG --threads=$(nproc)

                    -   name: 'PHPStan'
                        run: vendor/bin/phpstan analyze --no-progress --error-format=github --ansi --configuration=phpstan.dist.neon
        steps:
            -   name: 'Checkout Code'
                uses: actions/checkout@v4

            -   name: 'Setup PHP'
                uses: shivammathur/setup-php@v2
                with:
                    php-version: ${{ matrix.php }}
                    coverage: none
                    tools: composer:v2

            -   name: 'Install Dependencies'
                uses: "ramsey/composer-install@v2"
                with:
                    composer-options: "--optimize-autoloader"
                    dependency-versions: ${{ matrix.dependencies }}

            -   run: PHP_VERSION=${{ matrix.php }} ${{ matrix.actions.run }}
    test:
        name: PHPUnit
        needs: composer-validate
        runs-on: ubuntu-latest
        strategy:
            matrix:
                php: [ '8.1', '8.5' ]
                dependencies: [ highest, lowest ]
        steps:
            -   name: 'Checkout Code'
                uses: actions/checkout@v4

            -   name: 'Setup PHP'
                uses: shivammathur/setup-php@v2
                with:
                    php-version: ${{ matrix.php }}
                    coverage: none
                    tools: composer:v2

            -   name: 'Install Dependencies'
                uses: "ramsey/composer-install@v2"
                with:
                    composer-options: "--optimize-autoloader"
                    dependency-versions: ${{ matrix.dependencies }}

            -   name: 'Run PHPUnit'
                run: vendor/bin/phpunit --testdox --colors=always --configuration=phpunit.xml.dist

    code-coverage:
        name: PHPUnit Coverage
        needs: [ test, static-analysis ]
        runs-on: ubuntu-latest
        steps:
            -   name: 'Checkout Code'
                uses: actions/checkout@v4

            -   name: 'Setup PHP'
                uses: shivammathur/setup-php@v2
                with:
                    php-version: 8.5
                    coverage: xdebug
                    tools: composer:v2

            -   name: 'Install Dependencies'
                uses: "ramsey/composer-install@v2"
                with:
                    composer-options: "--optimize-autoloader"

            -   name: 'Run PHPUnit with Coverage'
                run: vendor/bin/phpunit --configuration=phpunit.xml.dist --coverage-clover=coverage.xml

            -   name: 'Upload coverage reports to Codecov'
                uses: codecov/codecov-action@v4
                with:
                    fail_ci_if_error: true
                    files: ./coverage.xml
                    token: ${{ secrets.CODECOV_TOKEN }}
